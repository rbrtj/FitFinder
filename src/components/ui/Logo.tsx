import { logoFont } from "~/utils/fonts";
const Logo = () => {
  return (
    <div className={`${logoFont.className} text-2xl uppercase tracking-widest`}>
      FitFinder
    </div>
  );
};

export default Logo;
